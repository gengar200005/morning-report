# Morning Report Phase 2 — Python dependencies

# Core pipeline
Jinja2>=3.1
requests>=2.31
pytz>=2024.1
holidays>=0.50

# Data sources (기존 수집 스크립트 의존성)
# NOTE: FinanceDataReader는 Python 3.11 환경에서 wheel 매치 실패 이슈로 제외.
#       실제로 기존 수집 스크립트가 쓰지 않음. 필요 시 Python 버전 핀과 함께 재도입.
numpy>=1.26
pandas>=2.0
pyarrow>=14.0
PyYAML>=6.0
yfinance>=0.2
pykrx>=1.0

# Google Drive upload
google-api-python-client>=2.0
google-auth>=2.0
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.0

# Notion (Phase 2 Step 6 예정)
notion-client>=2.0

# Dev · test
pytest>=7.0
